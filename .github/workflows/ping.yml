name: Keeping the server awake 


on:
  schedule:
    - cron: "*/15 * * * *"

jobs:
  ping-server: 
      runs-on: ubuntu-latest
      steps: 
        - name: send request to server
          run: curl -X GET "https://addyourbooks.onrender.com" || echo "Server unreachable" 
    
    
